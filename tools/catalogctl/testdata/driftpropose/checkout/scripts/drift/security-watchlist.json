{
  "version": 1,
  "doctrine": "Fixture checkout copy for tools/catalogctl/internal/driftpropose tests (spec addendum A1, F4 'the fourth screen'; and 2026-07-20-ccp-oob-provisioning-import.md §5.3 screens 1/3). Mirrors the SHAPE of the real scripts/drift/security-watchlist.json, trimmed to exactly what these tests need to hit (aws_security_group, aws_iam_role) or deliberately miss (tags.*, root_block_device.* on aws_instance, aws_instance creation) — never hand-edited to make a test pass; extend deliberately alongside a new fixture case.",
  "resource_types": {
    "aws_security_group": {
      "attributes": ["*"],
      "why": "network reachability — ingress/egress rules"
    },
    "aws_iam_role": {
      "attributes": ["assume_role_policy"],
      "why": "role trust policy"
    }
  },
  "attribute_patterns": [
    { "pattern": "policy", "why": "resource policy = access grants (leaf 'policy' at any depth)" },
    { "pattern": "publicly_accessible", "why": "direct public exposure" }
  ],
  "creation_security_types": ["aws_iam_role", "aws_iam_access_key"]
}
