# __generated__ by Terraform
# Fixture: what -generate-config-out must NEVER let through to a PR — a
# sensitive attribute rendered as a real literal (values are invented).

# __generated__ by Terraform from "appdb-postgres"
resource "aws_db_instance" "appdb_postgres" {
  engine         = "postgres"
  identifier     = "appdb-postgres"
  instance_class = "db.t3.medium"
  password       = "fixture-not-a-real-secret"
  username       = "fixtureadmin"
}
